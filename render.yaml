services:
  - type: worker
    name: discord-bot
    runtime: python
    runtimeVersion: 3.12.8
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: DISCORD_TOKEN
        scope: run
        isFile: false
